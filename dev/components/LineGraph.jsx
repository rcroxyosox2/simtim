import React from 'react';
import Style from '../scss/LineGraph.scss';

class LineGraph extends React.Component{
    render(){

        console.log(firebase);

        return (
            <div className="LineGraph">Hello im the line graph</div>
        );
    }
}

export default LineGraph;
